/** @jsx React.DOM */

var PageHeader = require("react-bootstrap/PageHeader");

var Plan = React.createClass({
    render: function() {
        return(
            <PageHeader>Plan Administration</PageHeader>
        );
    }
})

module.exports = Plan;
